string reverseWords(string s) {
	stringstream ss(s), result, word;
	while (ss >> word) {
		reverse(word.begin(), word.end());
		result << word << " ";
	}
	string res = result.str();
	return res.substr(0, res.size() - 1);
}