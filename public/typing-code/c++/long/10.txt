vector<int> plusOne(vector<int>& digits) {
	for (int i = digits.size() - 1; i >= 0; i--) {
		if (digits[i] + 1 == 10) {
			if (i == 0) {
				vector<int> arr(digits.size() + 1, 0);
				arr[0] = 1;
				return arr;
			}
			digits[i] = 0;
		} else {
			digits[i]++;
			return digits;
		}
	}
	return digits;
}